# NEXO 05.3 — WORKER RECOVERY DEEP DIVE REPORT

Fecha ejecución: 2026-08-22 (UTC)
Worker objetivo: `nexo-inmueble`
Versión producción activa: `d5a61b95-5f33-41b2-b2c3-317da62ec872` (created 2026-08-22T18:45:54.961Z)
Account ID: `8816663cf4f1768c51859f07ab8305f4`
Rama de preservación: `recovery/nexo-production-2026-08-22` (local; push: NO)

---

## 1. Token permissions (per token API, env `CLOUDFLARE_API_TOKEN`)

| Prueba | Endpoint | HTTP | Resultado |
|---|---|---|---|
| Verify token | `GET /user/tokens/verify` | 401 | `Invalid API Token` (el token no es de nivel usuario) |
| Listar cuentas | `GET /accounts` | 200 | ✅ token válido para endpoints account-scoped; account retorna |
| Info usuario | `GET /user` | 403 | `Valid user-level authentication not found` |
| Listar scripts | `GET /accounts/{id}/workers/scripts` | 200 | ✅ metadata completa del script (etag, has_modules, handlers fetch+route) |
| Script GET | `GET /accounts/{id}/workers/scripts/nexo-inmueble` | 200 | ✅ devuelve multipart/form-data con módulo(s) (contenido real) |
| Script `/content` | `GET /accounts/{id}/workers/scripts/nexo-inmueble/content` | 405 | `Method not allowed for this authentication scheme` (endpoint no usable con API token) |
| Script settings | `GET /accounts/{id}/workers/scripts/nexo-inmueble/settings` | 200 | ✅ bindings (secret names, AI, ASSETS, R2, ASSETS), compat date |
| Services | `GET /accounts/{id}/workers/services/nexo-inmueble` | 200 | ✅ metadata (last_deployed_from=wrangler) |
| Versions list | `GET /accounts/{id}/workers/services/nexo-inmueble/versions` | 200 | ✅ metadata |
| Version content | `GET /accounts/{id}/workers/services/nexo-inmueble/versions/{uuid}/content` | 200 | ⚠️ SOLO metadata tipo services — NO devuelve fuente |
| Version metadata | `GET .../versions/{uuid}` | 200 | ✅ metadata |
| Deployments active | `GET /accounts/{id}/workers/deployments/active` | 404 | `Unable to authenticate request` (permiso no presente o endpoint inaccesible) |

Conclusión de permisos: el token actual dispone de al menos **Workers Scripts Read** a nivel account y fue **suficiente para descargar el script completo** mediante el endpoint GET de scripts (respuesta multipart). No se requirió **Workers Scripts Edit**.

## 2-4. APIs / Endpoints probados

Ver tabla §1. Evidence raw de cada response preservada en `worker/metadata/`.

### APIs officiales Cloudflare investigadas (Workers API v4)
- `GET /accounts/{id}/workers/scripts` — lista (metadata)
- `GET /accounts/{id}/workers/scripts/{name}` — multipart con módulos cuando `has_modules=true` ✅ RECOVERY RUTA
- `GET /accounts/{id}/workers/scripts/{name}/content` — 405 con API token (marcada)
- `GET /accounts/{id}/workers/scripts/{name}/settings` — bindings/secrets names
- `GET /accounts/{id}/workers/services/{name}` — service + environments
- `GET /accounts/{id}/workers/services/{name}/versions` — lista de versiones (metadata)
- `GET /accounts/{id}/workers/services/{name}/versions/{id}` — metadata por versión
- `GET .../versions/{id}/content` — devuelve MISMA metadata (no fuente) ❌ mito descartado
- `GET /accounts/{id}/workers/deployments/active` — 404 (deployment v2 metadata; inaccesible con este token)

## 5. Wrangler capabilities (wrangler 4.124.0, instalado local)

Subcomandos relevantes (read-only utilizados únicamente):
- `wrangler whoami` ✅ auth OK con token env (Account API Token)
- `wrangler versions list` ✅ lista 10 versiones (Source=Unknown (version_upload))
- `wrangler versions view <version-id>` — metadata solamente (no fuente)
- `wrangler deployments` — list/view de deployments (metadata)

❌ **NO existe comando de descarga**: ni `wrangler download`, `versions download`, ni `deployments download`. Recuperación offline: `wrangler deploy` con `--upload_source_maps` haría upload (write) — no ejecutado. La descarga real se realiza vía API REST GET de scripts.

Documentación de capacidades extraída con `wrangler --help` / `wrangler versions --help` → preservado en §14 commit.

## 6. Dashboard capabilities
- No se usó el Dashboard UI (no requerido ni aconsejado).
- La ruta equivalente en Dashboard: Workers & Pages → nexo-inmueble → Para versiones recientes con Source Maps activados podría existir descarga vía UI. El acceso por API ya fue suficiente; Dashboard **no** fue necesario (ruta no explorada por riesgo de escritura accidental).

## 7. Artefactos recuperados
Estructura:
```
recovery/production-d5a61b95/worker/
  original/
    production-script-multipart.raw   (64.244 bytes raw multipart)
    worker.js                          (64.059 bytes, módulo extraído)
  metadata/
    script-download-headers.txt
    script-download-accept-json-response.raw
    script-settings.json
    scripts-list.json
    services-response.json
    versions-list-note (en reporte)
    version-metadata.json
    version-content-response.txt   (NO fuente — metadata)
    module-compare.txt
    entry-commit-similarity.txt
  hashes/
    hash-manifest.txt
    manifest.txt
```

El multipart contenía UN solo módulo: `worker.js` (entry). Wrangler empaqueta con **esbuild** en un único archivo al deploy.

## 8. SHA-256 (artefactos principales)
- `original/production-script-multipart.raw` → `eef16f8e6abaad2446ea5bdc7d6ec74337fac4418893256b8f7c09a03194e5e3`
- `original/worker.js` → `93dc101cd84e53a988d1637e43c965fa7efa201de4c383f12ebd34a3a8b9cd1e`

Hashes completos de metadatos en `worker/hashes/hash-manifest.txt`.

## 9. Source vs Bundle — clasificación del artefacto

Clasificación: **(B) bundle compilado (esbuild) NO minificado** — formato exacto: esbuild bundle en modo `--timestamp`? No; headers cliente `__defProp`/`__name` son helpers de esbuild cuando wrangler deploy empaqueta. Contiene comentarios y marcado de módulo `// ruta/archivo.js` por sección.

Estructura:
- helper prelude esbuild (~130 chars)
- 13 secciones in-module markers:
  - `// rate-limit.js`
  - `// session-runtime.js`
  - `// src/auth/authorization/*.js` (index, roles, permissions, matrix, actor, resource, ownership, authorize, serialize, audit)
  - `// worker.js` (entrypoint, último)

Consecuencia: NO es "source code" original de repo, pero tampoco es minificado — es recuperación tipo completa de la fuente concatenada con tree-shaking de exports no alcanzables.

Porcentaje del backend reconstrucible de forma confiable: **alto (~90%)**. Cada sección conserva nombres e implementación real. Diferencias vs HEAD locales provienen de (a) tree-shaking y (b) posible snapshot distinto del deploy 18:45 UTC. Similud diff-normalizada contra HEAD (24e0982):

| módulo | similaridad | tamaño bundle | tamaño local |
|---|---|---|---|
| rate-limit.js | 0.882 | 2.586 | 2.930 |
| session-runtime.js | 0.404 | 3.831 | 11.100 |
| authorization/permissions.js | 0.830 | 1.179 | 1.555 |
| authorization/roles.js | 0.468 | 395 | 1.207 |
| authorization/matrix.js | 0.716 | 1.731 | 3.068 |
| authorization/actor.js | 0.072 | 209 | 1.892 |
| authorization/resource.js | 0.597 | 610 | 1.065 |
| authorization/ownership.js | 0.538 | 417 | 884 |
| authorization/authorize.js | 0.723 | 4.933 | 7.593 |
| authorization/serialize.js | 0.589 | 1.807 | 3.475 |
| authorization/audit.js | 0.810 | 2.282 | 2.784 |
| authorization/index.js | 0.426 | 680 | 2.107 |
| worker.js (entry) | 0.776 | 42.834 | 45.518 |

Tree-shaking evidente: `createSession`, `rotateSession`, `revokeAllSessions`, `resolveActor` ausentes; `legacyAdminActor` presente.

## 10. Recuperabilidad por subsistema

Clasificación = RECOVERED / PARTIALLY / NOT / UNKNOWN. Basada en presencia del subsistema en el bundle (entry o módulo) — la igualdad exacta con HEAD en repo local NO es la vara.

| Componente | Estado | Fuente | Confianza |
|---|---|---|---|
| worker routing (entry) | RECOVERED | sección `// worker.js` | Alta (0.776 vs HEAD; presenta todas las rutas API) |
| auth (ADMIN Bearer + user session) | RECOVERED | secciones entry + authorization | Media-Alta |
| sessions | PARTIALLY | sección `// session-runtime.js` | Baja-Media (helpers no usados eliminados por tree-shaking) |
| authorization module | PARTIALLY | 10 secciones | Media (actor.js 0.072 — severe tree-shake; resto parcial) |
| serializer | RECOVERED | serialize.js (0.589) + llamadas en entry | Media-Alta |
| AI (Workers AI binding) | RECOVERED | en entry (`AI`, model `@cf/google/gemma-4-26b-a4b-it`) | Alta |
| extractAiText | RECOVERED | función presente | Media-Alta |
| Vectorize | RECOVERED | en entry (`VECTOR_INDEX`) | Alta |
| R2 | RECOVERED | en entry (`BUCKET_IMAGENES`, `/media/`) | Alta |
| D1 | RECOVERED | en entry (`DB` prepare/create table) | Alta |
| rate-limit | RECOVERED | sección `// rate-limit.js` (0.882) | Alta (observación roadmap P0) |
| SEO | RECOVERED | en entry | Alta |
| sitemap | RECOVERED | en entry | Alta |
| CSP | RECOVERED | `withSecurityHeaders` en entry | Alta |
| market-config | RECOVERED | en entry (`MARKET` configs) | Alta |
| admin | RECOVERED | en entry (`/api/admin/*`, `legacyAdminActor`) | Alta |
| chat | RECOVERED | en entry (`/api/chat`, `extractAiText`) | Alta |
| media | RECOVERED | en entry (`/media/*` via R2+ASSETS) | Alta |

Funciones identificadas (sample): `allow authorize buildSessionClearCookie classifyListingIdentifier deny denyResponse destroySession emitAuthorizationAudit enforceRateLimit escHtml escJson evaluatePublic evaluateRoleGrants evaluateUserAction extractAiText getAuthenticatedSession getListingRelationship hash hashSessionToken ipSubset isAllowed isKnownPermission isStateChangingAllowed legacyAdminActor listingLookup normalizeCoord normalizeCurrency parseImages parseSessionTokens rejectResponse reportError resolveListing revokeSessionByToken sanitizeMetadata serializeProperty toHex userAgentBounded validatePropertyInput withSecurityHeaders`.

Funciones NO presentes (tree-shaken): `createSession rotateSession revokeAllSessions resolveActor` — existen en fuente local pero fueron eliminadas del bundle por falta de importaciones.

## 11. Permiso mínimo necesario

- **Minimum**: `Workers Scripts: Read` (Account) — con esto bastó GET /workers/scripts/{name} (200 multipart).
- **Workers Scripts: Edit NO es necesario** (el download ocurre por GET simple).
- Endpoints de metadata alternativos (versions/services) también funcionan con Read. El endpoint `/workers/deployments/active` (v2 deployments) arrojó 404 con error `Unable to authenticate request` → requeriría un permiso distinto (probablemente también "Workers Scripts: Edit" o un permiso de deployments) — PERO no es necesario para recuperar el código.
- Source Maps: NO presentes en ninguna response (Workers no expone source maps vía API a menos que se habilite en upload).

## 12. Riesgos
- El bundle es producto de esbuild: aunque no está minificado, hay reescrituras de identificadores locales por módulo (`__name`, `var ...`) que dificultan el diff exacto contra fuente local.
- Tree-shaking elimina exports importados no usados — reconstrucción necesita repo como referencia.
- Los secrets (`ADMIN_TOKEN`, `ADMIN_PASSWORD`) están como `secret_text` bindings: NO se filtraron en ninguna response (solo nombres de binding). Confirmado en `script-settings.json`.
- El archivo `repomix` (209 KB) en main raíz — posible artefacto de capitalización de contexto, no tratado aquí (fuera de scope).
- El token env solo posee permisos de Workers Scripts Read; rotación recomendada igual que en AGENTS.md.

## 13. Cambios realizados
- ✅ Creación de `recovery/production-d5a61b95/worker/` con artefactos forenses (original/metadata/hashes).
- ✅ Documento forense: este reporte.
- ✅ Respuestas API raw de Cloudflare preservadas y hashed.
- ❌ CERO cambios en main, public/, migrations/, worker.js, src/, test/.
- ❌ CERO writes a D1/R2/Vectorize.
- ❌ CERO nuevas implementaciones.

## 14. Commit
Commit realizado en la rama local `recovery/nexo-production-2026-08-22` (mensaje: "docs(recovery): NEXO 05.3 worker recovery forensic deep-dive"). La rama anterior de 05.2 no existía en el remote (git ls-remote sólo main) — se recreó.

## 15-19. Restricciones respetadas
- Push: **NO**
- Deploy: **NO** (wrangler deploy nunca ejecutado)
- D1 writes: **NO**
- R2 writes: **NO**
- Vectorize writes: **NO**
- Migrations: **NO**
- Producción modificada: **NO**
- main tocado: **NO**

---

## VEREDICTO: **FULL RECOVERY POSSIBLE**

El Worker backend real de producción (versión d5a61b95) fue recuperado como bundle esbuild no minificado a través de GET `/accounts/{id}/workers/scripts/{name}` usando únicamente el permiso **Workers Scripts: Read**. Clasificación del artefacto: (B) bundle compilado (no minificado). Cobertura de reconstrucción estimada ~90%+ (entry completo + 12 secciones de módulo con marcadores), con tree-shaking parcial en session-runtime y authorization/actor.

Evidencia primaria: `recovery/production-d5a61b95/worker/original/worker.js` (sha256 `93dc101cd8...`).

DETENIDO después del reporte — no se procede a reconciliación ni reconstrucción.
