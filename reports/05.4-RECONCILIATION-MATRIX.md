# NEXO 05.4 — RECONCILIATION MATRIX

Baseline (verify): `24e0982` main HEAD (cd7e98b no existe en historial accesible)
Production bundle: `d5a61b95-5f33-41b2-b2c3-317da62ec872` (recuperado 05.3)
Production assets: recuperados previamente (fuera del alcance de esta fase)
Reconstructed source: `recovery/reconstructed/`

## Leyenda
- **ADDED IN PRODUCTION**: existe en producción pero NO en baseline
- **MODIFIED IN PRODUCTION**: existe en ambos con comportamiento distinto
- **REMOVED BY TREE-SHAKING**: eliminado por esbuild (no funcional)
- **RENAMED/REWRITTEN BY BUILD**: cambio por esbuild (no funcional)
- **UNCHANGED**: idéntico módulo formato-extractions
- **UNKNOWN**: sin evidencia suficiente

## Matriz por subsistema

| Subsystem | Producción evidencia | Baseline | Clasificación | Source recoverable | Confidence |
|---|---|---|---|---|---|
| routing (fetch handler) | entry module, todas las rutas presentes | `worker.js` HEAD | MODIFIED (features extra) | FULL | Alta (0.99 rebuild sim) |
| D1 (routing/queries) | `env.DB` prepare; SELECT con `currency`, placa_libre, gas_calle, agua_247, pago_exterior | columnas menos extensas | ADDED (columnas) | FULL | Alta |
| D1 (schema) | tracker 0001-0006 vs repo 0001-0005 | 0005 last | ADDED (migration 0006) | PARTIAL | Media (006 sin archivo) |
| R2 | `BUCKET_IMAGENES` fetch+head+webp fallback | idéntico | UNCHANGED | FULL | Alta |
| Vectorize | `VECTOR_INDEX` upsert/query/deleteByIds | HEAD usa `VECTORIZE` (roto) | MODIFIED (binding fix) | FULL | Alta |
| Workers AI | `AI` run con `@cf/google/gemma-4-26b-a4b-it` | idéntico | UNCHANGED | FULL | Alta |
| chat | `/api/chat` con Vectorize + fallback + `extractAiText` | usa `aiResponse.response` directo | MODIFIED | FULL | Alta |
| extractAiText | helper nuevo (fallbacks response/choices/result/text) | ausente | ADDED | FULL | Alta (producción extraída) |
| market config | `MARKET_CONFIG` + env overrides (`market_country` etc) | solo `whatsapp_phone` | ADDED | FULL | Alta |
| auth | Bearer `ADMIN_TOKEN` via `timingSafeEqual`; `legacyAdminActor` | igual | UNCHANGED | FULL | Alta |
| sessions | `/api/session/status,logout`; helpers tree-shaken | contrato completo local | REMOVED TREE-SHAKE | FULL (desde HEAD) | Alta |
| authorization | `authorize()` + matrix + actor legacy | idéntico, `resolveActor` tree-shaken | UNCHANGED | FULL | Alta |
| serializer | `serializeProperty` whitelist audiences | tree-shaken `propertyAudienceFor` | UNCHANGED | FULL | Alta |
| rate limit | `enforceRateLimit` + rejected headers | tree-shaken helper `LIMIT_DEF` ref | UNCHANGED | FULL | Alta |
| admin | POST/PUT/DELETE/verify; verify rate-limited nuevo | verify sin rate-limit | MODIFIED (RATE-LIMIT ADDED) | FULL | Alta |
| SEO | enrich canonical/og/geo/currencyJSON-LD | básico | MODIFIED (enriquecido) | FULL | Alta |
| sitemap | endpoint `/sitemap.xml` nuevo | ausente | ADDED | FULL | Alta |
| CSP | 9 hashes sha256 script-src + headers | idénticos (hashes <-> assets) | UNCHANGED | FULL | Alta |
| media | `/media/*` via R2+`ASSETS` fallback webp | idéntico | UNCHANGED | FULL | Alta |
| property | SEARCH ampliado (minPrice/neighborhood/q), SELECT +currency | básico | MODIFIED | FULL | Alta |
| config | `/api/config` devuelve MARKET_CONFIG | solo whatsapp_phone | MODIFIED | FULL | Alta |
| assets públicos | public/ (CSS/JS/HTML/páginas) | public/ HEAD + sitemap refs nuevas | UNKNOWN* | PARTIAL | Media (04.7/04.8 preservado, no verificado 05.4) |

\* Los assets 04.7/04.8 se preservaron separadamente (fuera de esta reconciliation). El bundle no incluye assets (solo bindings `ASSETS`).

## Resumen build-comparison (FASE 6)

Rebuilt con esbuild 0.28.1 (misma versión que wrangler 4.124 empaqueta):

| Métrica | Resultado |
|---|---|
| routes (paths /api/*, /sitemap.xml, /media/, /property.html) | **IGUALES** ✓ |
| SQL statements (normalizado espacios) | **IGUALES** ✓ |
| identificadores (const/let/var/function) | DIFIÉR solamente en `__defProp`, `__name` (helpers esbuild) ✓ |
| similitud por sección | 0.865–1.0 (roles/matrix exactos; entry 0.99) |
| wrangler deploy --dry-run | OK (bindings correctos, 62.42 KiB) |

## Flujo de diferencias clasificadas (entry)

- **REMOVED BY TREE-SHAKING**: imports `LIMIT_DEF` (unused)
- **RENAMED/REWRITTEN BY BUILD**: `__name()`, `/* @__PURE__ */`, escapes \x, PUBLIC_CODE_RE→PUBLIC_CODE_RE2, comentarios, shim helpers
- **ADDED IN PRODUCTION**: sitemap.xml, extractAiText, MARKET_CONFIG, minPrice/neighborhood/q, columnas currency/placa_libre/gas_calle/agua_247/pago_exterior, SEO geo/canonical/currency, rate-limit en admin verify, fallback teléfono real
- **MODIFIED IN PRODUCTION**: VECTORIZE→VECTOR_INDEX, `/api/config` payload, /api/chat extracción, normalizeCurrency en POST/PUT, SEO JSON-LD enriquecido
- **UNCHANGED**: rate-limit, auth, sessions, authorization, serializer, audit, ownership, resource, roles, matrix, permissions, index
- **UNKNOWN**: nada en entry; assets (validados 04.7/04.8 por separado)

## Migración drift
- Producción has `0006_properties_currency.sql` aplicada — NO existe en repo `migrations/` ni en `schema.sql`. CRITICO para reconciliación DB (no-worker, pero impacta al SQL del Worker: el Worker referencia `currency`).

## Tests (FASE 7)

| Registro | Estado |
|---|---|
| Historical 211 tests | **NOT RECOVERABLE** (no existen en repo; sin metadatos recuperables) |
| Current HEAD tests | **AVAILABLE**: 188/188 PASS (node --test test/*.test.mjs) |
| Production behaviors (sitemap, minPrice, q, currency, extractAiText, market config, canonical/og) | **NEED COVERAGE**: no tests existen en HEAD; deben derivarse en fases futuras |

No se creó suite masiva en esta fase (REGLA).

## Seguridad (FASE 9)

Verificado contra el bundle + metadatos:
- ✔ Secrets NO incluidos (ADMIN_TOKEN/ADMIN_PASSWORD aparecen solo como nombres de binding en settings; nunca como valores)
- ✔ SENTRY_DSN no recuperado (env var vacía; uso es dinánico opcional con parsing)
- ✔ WHATSAPP_PHONE tratado como configuración: valores públicos (`+5358385702` en producción) reproducidos en `recovery/reconstructed/wrangler.toml`
- ✔ CSP hashes public script-src → contenido de scripts públicos (no materia secreta)
- ✔ owner data (owner_name/owner_phone/internal_notes/contact_email/address) NO incorporada al código como string — solo SELECT explícito con whitelist auditada
- ✔ Long-hex en bundle = CSP `sha256-*` tokens (públicos hashes de scripts)
- ✔ Catálogo público no filtra campos privados (whitelist serializeProperty PUBLIC + SELECT público explícito)

## Reconstruction tree

`recovery/reconstructed/`:
- `worker.js` (reconstruido del entry: imports restaurados, helpers eliminados, PUBLIC_CODE_RE renamed, escapes decodificados, export restaurado)
- `rate-limit.js`, `session-runtime.js`, `src/auth/authorization/*.js` (idénticos a HEAD - UNCHANGED)
- `wrangler.toml` (WHATSAPP_PHONE = producción, bindings idénticos)
- `public/` (assets HEAD — representación; ASSETS reales de producción recuperados separadamente)
- `_reconstruction-ops.json` (bitácora de transformaciones deterministas)

## Veredicto FASE 10 — GATE

**A — RECONCILIATION READY**

- El bundle de producción es completamente descomponible (13 secciones con marcadores).
- La fuente reconstruida en `recovery/reconstructed/` produce, tras rebuild con esbuild 0.28.1, un bundle con rutas y SQL **idénticos** y diferencias únicamente de helpers esbuild.
- 188/188 tests baseline PASS sin modificación.
- Divergencia real identificada: funcionalidades NO-commiteadas desplegadas (sitemap, extractAiText, MARKET config, filtros extra, SEO enriquecido, currency column, rate-limit admin verify) + migration 0006 no versionada.
- Riesgo residual: drifting de assets (público) no verificado en esta fase; tests de behaviors nuevos pendientes.
