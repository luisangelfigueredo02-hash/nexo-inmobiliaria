# NEXO 05.5 — TEST COVERAGE GAP

## Posición oficial

- **Historical 211 tests = NOT RECOVERABLE.** No existen en ningún commit accesible del repo (superficie hasta `24e0982` muestra las mismas 8 suites), ni en artefactos recuperados de Cloudflare (metadatos/script-settings no contienen tests). No deben reconstruirse como si fueran recuperados.
- **Current repository tests = 188/188 PASS** (`npm test` → `node --test test/*.test.mjs`): authorization (59), identity-schema, listing-identity, rate-limit, security-headers, session-runtime, worker-integrity, worker.
- Ningún test actual menciona: `sitemap`, `extractAiText`, `MARKET_CONFIG`, `minPrice`, `normalizeCurrency`, `q` (search), `og:url`, `priceCurrency`, `enriched SEO`, `/api/config` payload extendido, admin-verify rate-limit, `VECTOR_INDEX` binding.

## Gap matrix (producción-only behaviors)

| # | Comportamiento | Código | Verificación posible | Prioridad |
|---|---|---|---|---|
| 1 | Endpoint `/sitemap.xml` (statics + property per public published) | worker.js rama explícita | Tests HTTP con DB mock: contiene `/, /mapa/, /comparar/, /ia/` + `/property.html?id=N-…` para cada published | P1 (SEO surface) |
| 2 | `extractAiText` chain fallbacks (response → choices[0].message.content → result → text → '') | función entry | Unit tests pura sobre helper | P1 (chat estabilidad) |
| 3 | `/api/config` devuelve MARKET_CONFIG completo | rama config | HTTP: contrato `{whatsapp_phone, market_country, market_locale, default_currency, map_center, map_zoom}` | P1 (cliente depende) |
| 4 | Filtros catálogo `minPrice`/`neighborhood` | rama properties GET | HTTP con DB mock: query contains `price >= ?`, `LOWER(neighborhood) = LOWER(?)` | P2 |
| 5 | Búsqueda `q` multi-token ≤6, LIKE 4 campos (título+3 geo+descripción) | rama properties GET | HTTP: joins OR + splitting + trim/escape `%_` + límite 6 tokens | P2 (seguridad LIKE-escape) |
| 6 | Columna `currency` en SELECT públicos + validación `ALLOWED_CURRENCIES` {USD,EUR,CUP} + `normalizeCurrency` en POST/PUT | validatePropertyInput + ramas admin | Unit validator + HTTP admin create/update | P1 (integridad datos) |
| 7 | SEO enriquecido: canonical, og:url, og:locale=es_CU, og:site_name=NEXO, JSON-LD `geo` con latitude/longitude, `priceCurrency` dinámico, `addressCountry:'CU',` fallback imagen → origen absoluto `/icons/icon-512.png` | SEO interceptor | HTTP sobre HTML generado: asserts por bloque/meta | P2 |
| 8 | `VECTOR_INDEX` binding (upsert/query/deleteByIds) vs anterior `VECTORIZE` roto | ramas POST/PUT/DELETE/chat | Mock env.VECTOR_INDEX: espera calls con ids = public_code | P1 (func. crítica) |
| 9 | Admin verify con rate-limit (`enforceRateLimit` antes de chequear tokens) | rama admin verify | HTTP: azote + doble verificación (401 vs 200 posteriores) | P2 |
| 10 | WhatsApp fallback real `+5358385702` (valor público) | const WHATSAPP_PHONE | HTTP con env vacío y con override | P3 |
| 11 | Assets production-only (SW v5 perf, nx-card__link etc.) | public/ assets | E2E-liviano: fetch de asset + hash match a canónica? (adec. fuera de tests) | P3 |
| 12 | XSS hardening (escHtml / escJson en interceptor) | entry + public/ | Cobeltura parcial existente (security-headers + worker) — revisar si SEO interceptor cubre campos HTML | — |
| 13 | Responsive fixes en tailwind-less CSS | variables.css/production | visual/manuales — fuera de tests | — |
| 14 | Media R2 negotiation requestedWidth fallback | rama /media/ | Cobertura existente en worker.test.mjs? (verificar) | — |

## Nota de cumplimiento

**No se escribió ningún test nuevo** (REGLA FASE 6). Este documento acotraliza únicamente el gap. La verificación de reproducibilidad correspondiente usó build-equivalence (FASE 5) con determinismo: rutas EQUAL, SQL EQUAL, CSP EQUAL, bindings EQUAL, tamaño 97.1%.
