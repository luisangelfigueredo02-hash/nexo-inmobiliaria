# NEXO 05.5 — SECURITY AUDIT (RECOVERED SOURCE)

Scope: `recovery/production-d5a61b95/` (bundle + assets) + `recovery/reconstructed/`. Scans estructurados (regex-based + revisión manual de ramas sensibles). **Resultado: no ROTATION REQUIRED findings.** Detalle a continuación.

## 1. Secrets / tokens / api keys / passwords

| Chequeo | Resultado |
|---|---|
| Hardcoded secrets (`api[_-]?key|secret|token|password|bearer|authorization` asignados a literal >12 chars) | **NONE** |
| Sentry DSN literal | **NONE** (uso opcional con parsing `env.SENTRY_DSN` — fail-silently si vacío) |
| ADMIN_TOKEN / ADMIN_PASSWORD | **NONE** (solo nombres de binding; competencia de seg compare `timingSafeEqual`) |
| Cloudflare account id `8816663…f4` | **Presente como identificador infra público en informes (no secreto; aparece en la URL del endpoint de API usada en 05.3)** |
| Tokens largos no-alfanuméricos | NONE (los strings alfanuméricos detectados son identificadores de CSP `sha256-…` o nombres de funciones) |

## 2. Datos privados de propietarios

- El bundle public-only SELECT eluntary columns (`public_code` list); campos owner/* solo en ramas admin (doble barrera 05: SELECT + serializeProperty('admin'))
- Comparaciones con whitelist auditada — verificadas en tests authorization.test.mjs 188 pass.
- HEAD y producción no exponen owner_name/owner_phone/internal_notes/address/contact_email en endpoints públicos.
- WhatsApp `+5358385702`: número de contacto público (business objective) — classificado como configuración, no credencial. Añadido en recovery/reconstructed/wrangler.toml y en assets. `+5351234567` aparece en admin.html como placeholder de formulario (no credencial, sólo marcador UI).

## 3. Endpoints debug / admin / auth bypass

- `/api/admin/verify`: Bearer con `timingSafeEqual`, ahora con `enforceRateLimit` (producción-only). Fail-closed: `denyResponse` 401/404 indistinguible.
- Plano admin: `legacyAdminActor` + `authorize()` fail-closed; audit `authorization_sensitive_allowed`. Sin diferencia que rompa el modelo 04.4.
- `/api/session/*`: CORS credencial solo en su superficie, Origin allowlist no incluye `null`, rate limited, no-store.
- `/api/chat`: rate-limit exigido antes de cualquier coste AI (valida en listing-identity test).
- `/api/config` devuelve sólo metadatos de mercado/UX (públicos).
- No debug endpoints extra en producción-only.

## 4. Código peligroso / SQL dinámico / XSS

| Peligro | Escaneo | Resultado |
|---|---|---|
| `eval` / `new Function` | regex sobre bundle y assets | **NONE** |
| Dynamic SQL via template `${…}SELECT|UPDATE|INSERT|DELETE|DROP` | regex | **NONE** (DLERA renders static strings, parámetros vía `.bind()`) |
| Column injection (`listingLookup`) | revisión | SAFE (decisión de columna por REGEXP / patrón N-X, nunca por valor arbitrario) |
| LIKE injection (`q` query) | revisión | SAFE (`[%_]` stripped, trimmed hasta 120 chars, token limit 6) |
| innerHTML sinks en assets | regex + revisión de interpolaciones | **TODAS** las interpolaciones pasan por `esc()`/`escapeHtml()` (helpers locales presentes en cada página verificada) |
| SEO interceptor | revisión | `escHtml` & `escJson` en todos los campos; `<\/script>` safely closed |

## 5. Hallazgos menores (no-blocking, NO INTENTADO a fix)

- `admin.html`: interpolación `${p.currency === 'CUP' ? ' CUP' : ''}` no pasa por `escapeHtml` — currency validade por allowlist USD/EUR/CUP arriba → vector cerrado. Documentada como nota.
- En assets production-only `sw.js` v5: cache verificación de freshness en `fetchPromise` — se mantuvo ojo.
- Sentry: no hay DSN activo real; producción es fail-open (`if (!env.SENTRY_DSN) return;`).

## 6. No ROTATION REQUIRED

Ningún secret real recuperado (sólo binding names). Secrets pendiente de rotación **desde contexto (problemas abiertos AGENTS)**: `~/.cf_token` / `~/.gh_token` en host de desarrollo. Deben rotarse en una fase autorizada — este cambio de scope NO está permitido en 05.5.
