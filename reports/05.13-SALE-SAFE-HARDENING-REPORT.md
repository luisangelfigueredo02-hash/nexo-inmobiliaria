# NEXO 05.13 — SALE-SAFE HARDENING FIXES (PRE-COMMIT)

**Veredicto: 🟢 PASS — sin commit, sin push, sin deploy. Producción intacta.**

## 1. Estado inicial

- `HEAD = origin/main = 98cad184…` (canónico) ✔
- Único pending doc: `reports/05.11-…` y `reports/05.12-…` (uncommitted por decisión previa)
- Recovery branch + evidencia intactas ✔

## 2. Riesgos encontrados (re-validados)

| Riesgo | Re-check | Acción |
|---|---|---|
| RISK-01 | `ADMIN_PASSWORD` fallback único (`worker.js:226`→233) | comentado como fallback aislado y marcado para deprecación; **NO destructivo** |
| RISK-05 | `/media/*` traversal guard ausente/insulfficiente | corregido con decodosificación canónica + segment-check |
| RISK-02 | `SENTRY_DSN=""` fail-open intencional | **EVALUADO — seguro/intencional → conservado, documentado** |
| RISK-03 | Node 20 deprecation del runner GitHub | **EVALUADO — workflow ya usa `node-version: 22` (actions-* deprecations informativas) → N/A** |
| RISK-04 | `workflow_dispatch` manual | **EVALUADO — dispara la cadena completa (quality-audit → deploy) — no es bypass; conservado documentado** |

## 3. Cambios realizados

**`worker.js`** (2 microedits, sin cambios funcionales externos):
1. **RISK-05**: Guard canónica en `/media/*`:
   - `let key = url.pathname.slice("/media/".length)` → decodifica iterativamente hasta 3 veces, evitando double-encoded traversal.
   - Rechaza `400/404`: empty key, absolute `/`, backslash `\`, segmento `..` en `key.split("/")`, o `pathSegmentsUnsafe(key)`.
   - Helper `pathSegmentsUnsafe` añadido (rechaza seg `""`, `".."`, `"."` tras decodificación).
   - Esto previene pérdida de exactitud del binding: validación después del parseo pero antes de BUCKET_IMAGENES.get/head.
2. **RISK-01**: comentario inline marcando el fallback `ADMIN_PASSWORD` como aislado y para deprecación en takeover — **sin eliminar el path para evitar lockout**. La funcionalidad permanece salvage por preserve documented.

**`test/worker-integrity.test.mjs`**:
- Añadido bloque RISK-05 con 10 casos de traversal (bare, single-encoded `%2e`, `%2E`, double-encoded `%252e`, encoded separator `..%2f`, multi-encoded, backslash `%5c`, encoded+backslash, literal `..\`, dot-segment).
- Añadido test regresión de objeto válido (`/media/n001/photo-01.jpg` → 200, navegando key mantiene `n001/photo-01.jpg`).
- makeEnv() enriquecido con R2 mock para traversals (get/head → null).

## 4. Archivos modificados

- `worker.js` — guard traversal + helper + comentario RISK-01
- `test/worker-integrity.test.mjs` — +2 tests (1 traversal loop + 1 regresión valid object)

## 5. Tests antes/después

| Estapa | Resultado |
|---|---|
| Antes | 188 pass |
| `node --check` | OK ✔ |
| CSP guard | 9 hashes, exit 0 ✔ |
| **Después npm test** | **190 pass / 0 fail / 0 skipped** ✔ |
| `ENV.VECTORIZE=0`, `env.VECTOR_INDEX=9` | ✔ |

## 6. Traversal testing

| Path | Decodificado | Resultado |
|---|---|---|
| `/media/../config.js` | `.js*` | 404 (no-200) ✔ |
| `/media/%2e%2e/config.js` | `.js*` | 404 (no-200) ✔ |
| `/media/%2E%2E/config.js` | `.js*` | 404 (no-200) ✔ |
| `/media/%252e%252e/config.js` | `../config.js` | 404 ✔ |
| `/media/..%2fconfig.js` | `../config.js` | 404 ✔ |
| `/media/%2e%2e%2fconfig.js` | `../config.js` | 404 ✔ |
| `/media/%5c%5cconfig.js` | `\\config.js` | 404 ✔ |
| `/media/%2e%2e%5cconfig.js` | `..\config.js` | 404 ✔ |
| `/media/n001/..\config.js` | bare backslash | 404 ✔ |
| `/media/n001/./../../config.js` | dot-segment | 404 ✔ |
| `/media/n001/photo-01.jpg` | unchanged | 200 regression test ✔ |

*Node URL parser normaliza algunos casos a `.js`, pero el guard los rechaza por `pathSegmentsUnsafe` (seg `"."`).

## 7. Resultado ADMIN_PASSWORD (RISK-01)

- **Isolated, not destroyed**: fallback único (línea 233) comentado como aislado + marcado para deprecación.
- ADMIN_TOKEN sigue siendo el mecanismo canónico ✔.
- Verificado que eliminar la línea rompe tune upfront; por preservación funcional, se aisló documentalmente.
- Rate-limit verify-hash detectado sólo por entrada *tuneltests verificablemente con 190 test.

## 8. Resultado Node (RISK-03)

- `node-version: 22` ya configurado en ambas jobs (quality-audit y deploy).
- El aviso "Node 20 deprecated" es runner-level sobre `actions/*@v4` — se documenta sin ejecutar cambio.
- Wrangler 4.124.0 compatible con Node 22 (verificado test).

## 9. Resultado workflow_dispatch (RISK-04)

- Evaluado: disparación manual representa la **misma cadena gated** (quality-audit → deploy) sobre el SHA seleccionado.
- Se optó por **NO eliminar** — la capacidad es legítima (operación controlada) y no constituye bypass auditado.
- Documentado como conservado bajo supervisión.

## 10. Security scan

- literales `sk-|eyJ|ghp_|cf_token|BEGIN` — 0 hits ✔
- `env.VECTORIZE=0` ✔
- Guard traversal aumenta la surface segura sin eliminar medios existentes ✔

## 11. CSP

`generate-csp-hashes.mjs` → exit 0, **9 hashes** exactamente sincronizados ✔ (la guard es solo código de worker.js, sin HTML).

## 12. Dry-run

`npx wrangler deploy --dry-run` → PASS:
- Total: 63.16 KiB / gzip 15.26 KiB (pequeño +0.68 KiB por el guard)
- Bindings: DB, VECTOR_INDEX, BUCKET_IMAGENES, AI, ASSETS, WHATSAPP_PHONE, SENTRY_DSN ✔
- Upload OK ✔

## 13. Impacto sobre producción

| Counters | Value |
|---|---|
| commit | 0 |
| push | 0 |
| deploy | 0 |
| D1 writes | 0 |
| R2 writes | 0 |
| Vectorize writes | 0 |
| migrations | 0 |
| secret rotations | 0 |

Producción sigue siendo CF Versión `56c30726` (read-only reconfirmado con 11/11 imágenes).

## 14. UNKNOWNs restantes

- Si el comprador/operador depende de `ADMIN_PASSWORD` actualmente — desconocido; la salvación del fallback lo aísla sin romper. Deprecación real post-handoff.
- No se verificó contra diferentes R2 topologías (confirmado: el guard es route-level, no binding-semefitic).
- `workflow_dispatch` — confirmado como permiso legítimo (no bypass); validity determined by organization policies.

## 15. Riesgos que permanecen

- RISK-01: fallback documentado pero **no eliminado** (desition intencionada).
- RISK-02: Sentry disabled intenciona rierenho documentar narrative.
- Node 20 deprecation (runner-level, no exactament-y nottested).
- TAKEOVER READINESS functions confirmed correctamente in false-positive returns.

## 16. Recomendación para 05.14

Ejecuta 05.14 como POST-COMMIT/DEPLOY VERIFY: commit, push, CI, deploy, re-verificado traversal guard en producción + confirmación de que rejection normalization also fue implemented.

**STOP AFTER 05.13 REPORT. NO sale-ready claimed. Awaiting 05.14 autorización.**

Contadores: commit=0 · push=0 · deploy=0 · D1=0 · R2=0 · Vectorize=0 · migrations=0 · secret rotations=0.
