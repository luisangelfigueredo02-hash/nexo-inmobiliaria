# NEXO 05.6 — PRE-MERGE REPORT

## 1. Git baseline (FASE 1)

- `HEAD` recovery = **74ad05ac53dd4b0007b9a9845932ebeb0fc51f42** (local, amended report docs)
- `main`/`origin/main` = **24e09827876cd4f1c48822e9eb373b50b403fb41** (START: INTACTO)
- Working tree limpio; branch recovery exists; no hassles unexpected.

## 2. Recovery inventory (FASE 2+3)

- CANONICAL APPLICATION SOURCE: `recovery/reconstructed/`
- CANONICAL PRODUCTION EVIDENCE: `recovery/production-d5a61b95/`
- MAIN: 24e0982 (legacy baseline)
- Inventory por clasificación en `reports/05.6-RECOVERY-INVENTORY.md`.

## 3. Merge simulation (FASE 4)

- Construct intact-to-noresize tree: 78 files ADDED, 0 modified, 0 deleted, 0 conflicts.
- Detalle: `reports/05.6-MERGE-SIMULATION.md`.

## 4. Critical conflict audit (FASE 5)

| Fichero/subsistema | Estado |
|---|---|
| worker.js | Canonical entry vs HEAD difieren — queda documentado y no activa el merge |
| wrangler.toml | Canonical vs HEAD difieren — queda documentado |
| migrations/ | Nada added; 0006 solo documented (safe) |
| package.json | Sin cambios |
| scripts/ | `generate-csp-hashes.mjs` intact bajo HEAD |
| public/ | Sin cambios (los assets canonical se mantienen en `recovery/production-d5a61b95/assets`) |
| src/auth/authorization/* | módulos canonical IDENTICAL a HEAD (verificado byte-a-byte) |
| session-runtime.js | identical (entry del recovery-addonly) |
| rate-limit.js | identical |
| CSP generation | rebuild identical |
| manifest.json, sw.js, variables.css | canonical assets (no conflicts) |
| API routes | parity con HEAD |
| **env.VECTORIZE vs env.VECTOR_INDEX** | HEAD usa VECTORIZE (roto); canonical usa VECTOR_INDEX (verificado 05.4) — preserve canonical |

## 5. D1 migration safety (FASE 6)

- HEAD `migrations/` tiene {0001…0005}; tracker producción {0001…0006}.
- `recovery/notes/0006_properties_currency.sql` es **documentation-only**: todo comentarios, sin CREATE ni ALTER ejecutable.
- HEAD schema genera por JOIN migrations/{0001,0005} — sin duplicación y sin data-loss.
- Toda actividad D1 en 05.6 estuvo read-only comprobada.

## 6. Test baseline (FASE 7)

- `node --check recovery/reconstructed/worker.js` OK
- `npm test`: **188 pass / 0 fail** (8 suites: authorization, identity-schema, listing-identity, rate-limit, security-headers, session-runtime, worker-integrity, worker)
- CURRENT RECOVERED/RECONSTRUCTED TEST BASELINE = 188.
- HISTORICAL 211 = NOT RECOVERABLE (per 05.5; no existen en HEAD ni no existen en production).

## 7. Build dry-run (FASE 8)

- `esbuild` -> `Total Upload: 62.42 KiB / gzip: 15.11 KiB` (= 05.4 evidence).
- Bindings: as HEAD (`wrangler deploy --dry-run` pasa).
- Determinism: re-build standardized strip path prefix → byte-identical a `build-evidence/esbuild-rebuilt.js`.

## 8. Production safety (FASE 9)

- Cloudflare writes = 0; D1 writes = 0; R2 writes = 0; Vectorize writes = 0.
- Version d5a61b95 no troceada. Solo infolectura.

## 9. Merge strategy (FASE 10)

**A) Fast-forward merge (git checkout main; git merge recovery)** **← recomendada**
- Razón: `recovery` is a direct descendant of `main` at commit HEAD→recovery branch (merge-back = 24e0982). No rebase needed; no files entraniert as modified. Preservess todo history.

B) Cherry-pick: incómodo (los commits recovery son lineage; se rompe registro autho vinculato).

C) Rebase recovery onto main: unnecessary and breaks timestamps.

## 10. Git workflow permanente (FASE 11)

Objetivo: *ninguna producción futura puede existir sin un SHA correspondiente*.

1. modify
2. test
3. commit
4. **push to remote**
5. CI (CI-lint or fail workflow case file in .github) — verification forced
6. deploy **only from known commit**
7. record Cloudflare Version ID after deploy
8. verify production (healthchecks)
9. associate Cloudflare Version ↔ Git SHA (persistent record)

Mecanism blocker for dirty-tree deploys (docs only):
```
assert [ -z "$(git status --porcelain)" ]
assert [ "$(git rev-parse HEAD)" = "$(git rev-parse @{u})" ]
npx wrangler deploy
```

## 11. Riesgos

- Lanzar segunda deploy desde un working tree sucio volverá a replicar el drift del 2026-08-22 → mitigado por la política del ciclo.
- Producción 0006=properties.currency + rate_limits runtime: por recovery/notes — afecta solo en documentación en esta fase.

## 12. Unknowns

- AUTORÍA del working-tree que causó el drift (FUENTE: version_upload). Evitable, queda documentado para auditar posteriormente.

## 13. Exact next command sequence

```
git checkout main
git merge recovery/nexo-production-2026-08-22
git rev-parse main        (record merge SHA)
git push origin main      (not allowed yet; por autorizar step-by-step)
```

## VEREDICTO: 🟢 MERGE-READY

Todos los puntos de control cumplidos: none real merge tree conflicts, tests OK, dry-run OK, production unchanged.

STOP AFTER THIS REPORT.
