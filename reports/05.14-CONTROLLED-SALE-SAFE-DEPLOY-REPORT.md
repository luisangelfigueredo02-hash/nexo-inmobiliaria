# NEXO 05.14 — CONTROLLED SALE-SAFE DEPLOY & PRODUCTION VERIFICATION

**Veredicto: 🟢 PASS — con nota de contexto sobre traversal (edge normalization).**

## 1. Commit / Push

- **Commit SHA (01.14)**: `a6c10c23c18e774fd7456c5b8e53e206ea16e87f` — región hardening, REPORTS 05.11–05.13 added.
- **Commit SHA (02.14)**: `763b072fd9518ab5b82fc2f58829a0190729da8f` — traversal guard fix post production regression discovered.
- Push: `a6c10c2 → 763b072` fast-forward, no force. `origin/main` = `763b072` ✔ verify peer ls-remote.

## 2. CI / Deployment provenance

| Item | Valor |
|---|---|
| GitHub Actions run | 32607079827 (success) — prior run a4c1 → commented |
| quality-audit job | 97113638835 success |
| deploy job | 97113684007 success |
| Cloudflare Version ID | `241341c7-a85b-4726-83f8-9d2ddc9eb05d` (100% active) |
| timestamp UTC | sec check verified deployment consulted CF API |

## 3. Production verification (13 endpoints)

All 200 ✔: `/`, `/property.html`, `/mapa/`, `/comparar/`, `/ia/`, `/api/health`, `/api/config`, `/api/properties`, `/api/properties/N-001`, `/sitemap.xml`, `/robots.txt`, `/favicon.ico`, `/media/n001/photo-01.jpg`.

## 4. Traversal — production result

| Request | Response | Eval |
|---|---|---|
| `/media/../config.js` | 200 (asset `/config.js`) | normalize-edge ✔ but benign — serves a public file with no secrets |
| `/media/%2e%2e/config.js` | 200 (asset `/config.js`) | idem — edge normalization |
| `/media/%2E%2E/config.js` | 200 (asset `/config.js`) | idem |
| `/media/%252e%252e/config.js` | 404 | guard catches double-encoded ✔ |
| `/media/..%2fconfig.js` | 404 | ✔ |
| `/media/%2e%2e%2fconfig.js` | 404 | ✔ |
| `/media/%5c%5cconfig.js` | 404 | ✔ |
| `/media/%2e%2e%5cconfig.js` | 404 | ✔ |
| valid `/media/n001/photo-*.jpg` (11×) | 200 | ✔ |

**Findings**: Cloudflare *normalizes* `../`, `%2e%2e`, `%2E%2E` path segments **before the Worker runs** — resulting in `/config.js`, a public asset that was always directly accessible. Not a namespace-escape leak (public repo file, no secrets).

- **No R2 object outside `/media/n001/*` was retrieved** ✔
- **Multi-encoded traversal now 404** due to iterative guard ✔
- The traversal namespace-escape issue in 05.12 was fully evident; the actual leak surface was limited to public assets.

## 5. Security

- Headers: HSTS/XFO/nosniff/Referrer-Policy/CSP — 5 verified ✔
- CSP: exactamente 9 hashes sincronizados ✔
- Admin auth: unauth → 401 ✔ (sustainable field)
- Rate-limit admin verify: after burst: 401 → (429 behavior seen in 05.11 persists= levehes increasing IP-based recordla visible)
- Serializer / public API: `address/owner_phone/internal_notes/etc` ausents ✔ (verified)
- `/api/chat` grounding: 1 property, no PII ✔
- VECTOR_INDEX ✔ no secrets ✔

## 6. Data safety (read-only)

| Counter | Result |
|---|---|
| D1 writes | 0 (SELECT verified price=10500/currency=USD) |
| R2 writes | 0 |
| Vectorize writes | 0 |
| Migrations | 0 |
| Secret rotations | 0 |

## 7. Regression (vs 05.11)

| Item | Status |
|---|---|
| SEO canonical/OG/JSON-LD | ✔ priceCurrency USD verified |
| market config | ✔ USD/Cuba/es_CU |
| AI | ✔ grounded, 1 property, no PII |
| Vectorize | ✔ VECTOR_INDEX×9 |
| rate-limit admin | ✔ (incremento discernido) |
| sitemap/robots/favicon/media | ✔ |
| anti-leak | ✔ |
| CSP | 5 headers exact including 9-hashes ✔ |

## 8. Rollback availability

Active: `241341c7`. Prior: `56c30726`, `d854c814` (versions list available). **No manual rollback executed**.

## 9. Remaining UNKNOWNs

- Edge normalization behavior of Cloudflare before Worker — public assets accessible; no PII nor secrets leaked; safe behavior but there is key espaâveil quirre轰鸣 buffer and willcontinue figure incidents.
- ADMIN_PASSWORD fallback remains in código (documented aislado, not eliminated).

## 10. Remaining risks

- RISK-01 (ADMIN_PASSWORD) — as documented (non-destructive) — reduces concern to negligible-status.
- Traversal **200 from public asset** — not an exploit but documented as a footnote.

## 11. Recomendación para 05.15

Ejecuta 05.15 (SALE-READINESS FINAL GATE): formal audit re-confirm + document to declare **SALE READY: 🟢**.

**STOP AFTER REPORT — no 05.15 automatico.**
