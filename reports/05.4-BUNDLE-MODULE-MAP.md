# NEXO 05.4 — BUNDLE MODULE MAP

Fuente: `recovery/production-d5a61b95/worker/original/worker.js`
Versión producción: `d5a61b95-5f33-41b2-b2c3-317da62ec872`
Baseline comparación: `24e0982` (main HEAD; `cd7e98b` no existe en historial accesible)
Pipeline: esbuild (Wrangler 4.124), no-minificado, con helpers `__name`, `__defProp`, comentarios `// ruta/modulo.js` por sección, `/* @__PURE__ */`, y escapes `\x..` para no-ASCII.

## Mapa de secciones (13 módulos + prelude)

| # | Sección | Tamaño | Origen en repo | Mensaje |
|---|---|---|---|---|
| 0 | prelude esbuild | 130c | helpers (`__defProp`,`__name`) | BUILD |
| 1 | `// rate-limit.js` | 2.586c, 69L | `rate-limit.js` | UNCHANGED (sim 0.88; ids comunes: 17) |
| 2 | `// session-runtime.js` | 3.831c, 86L | `session-runtime.js` | REACHED SUBSET (sim 0.40) |
| 3 | `// src/auth/authorization/permissions.js` | 1.179c | `permissions.js` | UNCHANGED (0.83) |
| 4 | `// src/auth/authorization/roles.js` | 395c | `roles.js` | REACHED SUBSET (0.47) |
| 5 | `// src/auth/authorization/matrix.js` | 1.731c | `matrix.js` | UNCHANGED (0.72) |
| 6 | `// src/auth/authorization/actor.js` | 209c | `actor.js` | SEVERE TREE-SHAKE (0.07) |
| 7 | `// src/auth/authorization/resource.js` | 610c | `resource.js` | UNCHANGED (0.60) |
| 8 | `// src/auth/authorization/ownership.js` | 417c | `ownership.js` | UNCHANGED (0.54) |
| 9 | `// src/auth/authorization/authorize.js` | 4.933c | `authorize.js` | UNCHANGED (0.72) |
| 10 | `// src/auth/authorization/serialize.js` | 1.807c | `serialize.js` | SUBSET (0.59) |
| 11 | `// src/auth/authorization/audit.js` | 2.282c | `audit.js` | UNCHANGED (0.81) |
| 12 | `// src/auth/authorization/index.js` | 680c | `index.js` | SUBSET (0.43) |
| 13 | `// worker.js` (entry) | 42.847c, ~910L | `worker.js` | PRODUCTION ≠ HEAD, features extra (0.78) |

## Tree-shaking — identificadores presentes en repo (HEAD) pero ELIMINADOS del bundle

Se mantienen en el arbol local pero producción nunca los ejecuta (contrato 04.2 §11; handoff de Authentication):

- `session-runtime.js`: `createSession`, `rotateSession`, `revokeSession`, `revokeAllSessions`, `generateSessionToken`, `buildSessionCookie`, `base64urlEncode`, `base64urlDecode`, `MAX_CONCURRENT_SESSIONS`, `TOKEN_BYTES`
- `authorization/actor.js`: `resolveActor` (uso actual = `legacyAdminActor`)
- `authorization/roles.js`: `anonymousActor`
- `authorization/serialize.js`: `propertyAudienceFor`

Clasificación: **REMOVED BY TREE-SHAKING** — no son cambios funcionales.

## Entry module — diferencias SOURCE-LEVEL entre producción y HEAD `24e0982`

La respuesta borde de HEAD 0.776 no cubría features; producción fue desplegada desde un working tree con cambios nunca commiteados. Todas las diferencias funcionales identificadas:

### ADDED IN PRODUCTION (no están en HEAD)
1. **endpoint `/sitemap.xml`** (nuevo): static [`/`, `/mapa/`, `/comparar/`, `/ia/`] + cada `property.html?id=<public_code>` publicado
2. **`extractAiText(aiResponse)`** helper con fallbacks: `response` → `choices[0].message.content` → `result` → `text` (evita 500s en `/api/chat`)
3. **`GET /api/config` devuelve `MARKET_CONFIG`** completo (`whatsapp_phone`, `market_country='Cuba'`, `market_locale='es_CU'`, `default_currency='USD'`, `map_center=[23.1136,-82.3666]`, `map_zoom=12`, env-overrides) — en HEAD solo `whatsapp_phone`
4. **Filtros catálogo ampliados** `/api/properties`: `minPrice`, `neighborhood`, y búsqueda `q` multi-token (title/neighborhood/city/province/description LIKE, ≤6 tokens)
5. **Columna `currency`** en SELECT públicos (list, detalle, similar, SEO, chat IA) + helper `normalizeCurrency(data.currency)` en POST/PUT
6. **SEO enriquecido**: `canonical`, `og:url`, `og:locale=es_CU`, `og:site_name=NEXO`, JSON-LD `geo` (coerce), `priceCurrency` de property.currency, `addressCountry: 'CU'`, imagen fallback → `/icons/icon-512.png` con origen absoluto
7. **Corrección binding Vectorize**: `VECTORIZE` (HEAD, roto) → `VECTOR_INDEX` (coincide con `wrangler.toml`). En HEAD las rutas chat/upsert/delete estaban rotas
8. **`/api/admin/verify` con rate limit** (`enforceRateLimit` antes del chequeo)
9. **Fallback WhatsApp real**: `"+5358385702"` (vs placeholder `"+5350000000"`) — público pero rebaja sanitize defaults

### RENAMED/REWRITTEN BY BUILD (no funcional)
- imports inlineados (secciones con marcadores `//`)
- `PUBLIC_CODE_RE` → `PUBLIC_CODE_RE2` (esbuild rename por colisión con `resource.js`)
- `__name(<f>, "<f>")` helpers tras cada función top-level
- comentarios eliminados; estilo de formateo esbuild; `Date.now()/1e3`; `999_000_000`→`999e6`
- no-ASCII (á, é, í, ó, ú) → escapes `\xNN`
- `/* @__PURE__ */` en Set/enclosures

### UNCHANGED (módulos no-entry)
`rate-limit.js`, `permissions.js`, `matrix.js`, `ownership.js`, `resource.js`, `authorize.js`, `audit.js`, `roles.js`, `serialize.js`, `index.js` — todos los identificadores visibles en el bundle existen también en HEAD (bundle-only = ∅).

## Producción DB — divergencia del tree de migrations

Read-only (`wrangler d1 execute --remote`):

- `d1_migrations` tracker: `0001..0006` aplicadas; `0006_properties_currency.sql` (2026-08-22 15:15:47) **NO existe en `migrations/` del repo**
- `properties` DDL producción: añade `currency TEXT` via ALTER residual (`"... created_by ..., currency TEXT"`)
- `schema.sql` (bootstrap local) ≠ producción también (sin currency)

Clasificación: **schema drift** — reconciliar antes de fases planificadas.

## Evidencia preservada
- Bundle raw: `recovery/production-d5a61b95/worker/original/production-script-multipart.raw`
- Entry extraction: `/tmp/entry-prod.js` (fase-análisis); diff completo `recovery/analysis/entry-vs-head.diff`
- Playground estructuras: `recovery/analysis/module-structure.json`, `module-diff-summary.json`
- DB prod (read-only): `recovery/production-d5a61b95/worker/metadata/prod-properties-schema.json`, `prod-d1-migrations.json`
