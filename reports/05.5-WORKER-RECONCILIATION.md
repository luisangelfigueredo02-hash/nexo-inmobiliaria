# NEXO 05.5 — WORKER RECONCILIATION (A vs B vs C)

- **A** = `recovery/production-d5a61b95/worker/original/worker.js` (bundle de producción d5a61b95, sha256 93dc101c…)
- **B** = `recovery/reconstructed/worker.js` (fuente canónica reconstruida, sha256 79d79bf2…)
- **C** = `worker.js` en HEAD main `24e0982` (INTACTO)

**Resultado de conjuntos:** funciones de C ⊂ funciones de A (HEAD entry no define ninguna función que producción no tenga). A añade respecto a C: `extractAiText`, `normalizeCurrency`. B ≡ A en conjunto de funciones e identificadores (excepto helpers esbuild): la reconstrucción es exacta a nivel de símbolos.

## Matriz por bloque de comportamiento

| Bloque | Clasificación | Detalle |
|---|---|---|
| Prelude de helpers esbuild (`__defProp`, `__name`) | BUILD-TRANSFORMATION | Solo en A; no existe en fuentes B/C; generado por bundling |
| Imports de entry (multilinea → inlineado) | BUILD-TRANSFORMATION | C tiene 3 imports; A los resuelve en secciones `//`; B los restauró |
| `PUBLIC_CODE_RE` → `PUBLIC_CODE_RE2` (colisión con resource.js) | BUILD-TRANSFORMATION | Solo renombre esbuild; B lo restauró a `PUBLIC_CODE_RE` |
| `function listingLookup` | PRODUCTION-EXACT | Byte-level distinto (formato esbuild) pero semánticamente idéntico en A/B/C |
| `function reportError` (Sentry) | PRODUCTION-EXACT | Igual semántica; formato esbuild |
| `function escHtml` / `function escJson` | PRODUCTION-EXACT | Igual semántica; formato esbuild |
| `function validatePropertyInput` | PRODUCTION-EXACT + additions | A añade `ALLOWED_CURRENCIES` validator (`currency ∈ {USD,EUR,CUP}`), ausente en C → fragmento **PRODUCTION-ONLY** |
| `function normalizeCoord` | PRODUCTION-EXACT | Semánticamente igual |
| `function toInt` | PRODUCTION-EXACT | Igual |
| **`function extractAiText`** | **PRODUCTION-ONLY** | Solo A y B; fuente recuperada únicamente del bundle; C no la tiene |
| **`function normalizeCurrency`** | **PRODUCTION-ONLY** | Solo A y B; `data.currency` normalizado en POST/PUT admin |
| Handler fetch (routing branch order) | PRODUCTION-EXACT | A/B/C mantienen el mismo orden de ramas; se añade una nueva rama **PRODUCTION-ONLY** para sitemap |
| `timingSafeEqual`, `isAdmin`, `authorizeAdminPlane`, `normalizeImages` closures | BUILD-TRANSFORMATION | En A: `/* @__PURE__ */ __name(() => {…}, "nombre")`; B restauradas a arrow plain |
| CORS allowlist + `corsHeaders` | PRODUCTION-EXACT | Igual |
| **Rama `/sitemap.xml`** | **PRODUCTION-ONLY** | Solo A y B; static pages `[/, /mapa/, /comparar/, /ia/]` + property.html por public_code publicado |
| SEO interceptor `/property.html` | PRODUCTION-EXACT + **PRODUCTION-ONLY additions** | A/B añaden: canonical, og:url, og:locale es_CU, og:site_name, JSON-LD `geo`, `priceCurrency` dinámico, `addressCountry: 'CU'`, fallback imagen `/icons/icon-512.png` con origen absoluto |
| Media R2 `/media/*` | PRODUCTION-EXACT | Formato distinto; semántica igual |
| **Rama `/api/config`** | **PRODUCTION-ONLY** | A/B: devuelve `MARKET_CONFIG` (whatsapp, country Cuba, locale es_CU, currency USD, map_center [23.1136,-82.3666], zoom 12); C: solo `{ whatsapp_phone }`. Constante `MARKET_CONFIG` (env overrides) — **PRODUCTION-ONLY** |
| `GET /api/properties` | PRODUCTION-EXACT + **PRODUCTION-ONLY extensions** | A/B: `minPrice`, `neighborhood`, búsqueda `q` (≤6 tokens LIKE title/neighborhood/city/province/description), SELECT con `currency`+flags+`created_at` |
| `/api/properties/:id/similar` | PRODUCTION-EXACT + columns | A/B SELECT añade `currency`, `placa_libre`, `gas_calle`, `agua_247`, `pago_exterior` |
| `/api/properties/:id` detalle | PRODUCTION-EXACT + columns | dicho |
| Admin verify con `enforceRateLimit` | **PRODUCTION-ONLY** | C carece del rate-limit en login admin |
| Admin GET list | PRODUCTION-EXACT + column `currency` | dicho |
| Admin POST | PRODUCTION-EXACT + currency/VECTOR_INDEX | A/B: `normalizeCurrency(data.currency)`, `env.VECTOR_INDEX`, secuencia listing_id_sequence idéntica |
| Admin PUT | PRODUCTION-EXACT + currency/VECTOR_INDEX | dicho |
| Admin DELETE | PRODUCTION-EXACT + VECTOR_INDEX | A/B usan `VECTOR_INDEX`; C `VECTORIZE` (roto en producción) |
| Chat `/api/chat` | PRODUCTION-EXACT + **PRODUCTION-ONLY** | A/B: `env.AI && env.VECTOR_INDEX`, respuesta via `extractAiText(aiResponse) \|\| null` (C: `aiResponse.response` directo), SELECT con columnas currency+flags |
| ASSETS passthrough + 404 | PRODUCTION-EXACT | Igual |
| `export default`/var worker_default | BUILD-TRANSFORMATION | A: `var worker_default = {}` + export aliased; B/C: `export default {}` |

## Constantes/build literals

| Lit | Clasificación |
|---|---|
| Escapes `\xE1` etc. por no-ASCII | BUILD-TRANSFORMATION (B restaura caracteres UTF-8) |
| `Date.now()/1e3` vs `Date.now()/1000` | BUILD-TRANSFORMATION (esbuild) |
| `999_000_000` → `999e6`, `100_000` → `1e5`,`5000`→`5e3` | BUILD-TRANSFORMATION (esbuild) |
| Comentarios eliminados | BUILD-TRANSFORMATION (esbuild) |
| `__name(f,"f")` tras cada función | BUILD-TRANSFORMATION (esbuild) |
| WHATSAPP fallback `+5358385702` en A/B vs `+5350000000` en C | **PRODUCTION-ONLY** (valor real de producción) |

## Tree-shaken (ausen en A; SÍ existen en HEAD módulos C)

- **HEAD-ONLY (irrelevante para producción):** `createSession`, `rotateSession`, `revokeSession`, `revokeAllSessions`, `generateSessionToken`, `buildSessionCookie`, `base64urlEncode/Decode`, `MAX_CONCURRENT_SESSIONS`, `TOKEN_BYTES`, `resolveActor`, `anonymousActor`, `propertyAudienceFor`, `LIMIT_DEF` usage. Clasificación: **HEAD-ONLY** (por tree-shaking esbuild; producción nunca los ejecuta).

## UNKNOWN

- Autoría/fecha exacta del working tree que añadió las diferencias PRODUCTION-ONLY (deployment por `version_upload` sin author).
- Por qué HEAD quedó por detrás (sin commitear) de producción.

## Declaración

No se eliminó información para igualar: los 12 módulos no-entry de B son byte-idénticos a HEAD (0.88–1.0 rebuild sim tras esbuild); la única fuente divergente real es el entry, y TODA su divergencia quedó clasificada arriba. Ninguna discrepancia queda sin clasificar.
