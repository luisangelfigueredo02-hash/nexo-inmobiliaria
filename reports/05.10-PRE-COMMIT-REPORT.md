# NEXO 05.10 — PRE-COMMIT REPORT

**Estado:** 🟢 PASS — promoción controlada preparada. **SIN commit, SIN push, SIN deploy.**

## 1. Estado Git inicial

| Check | Valor |
|---|---|
| `git branch --show-current` | `main` |
| `git rev-parse HEAD` | `d487892264a573f1896ce3c4b0cc3f150779dd10` |
| `git rev-parse origin/main` | `d487892264a573f1896ce3c4b0cc3f150779dd10` |
| `git status` inicial | working tree clean ✅ |
| Rama recovery | `recovery/nexo-production-2026-08-22` — intacta (no tocada) ✅ |
| Evidencia | `recovery/production-d5a61b95/` — intacta ✅ |

Nota: `reports/05.8-*` y `reports/05.9-*` **no existen en el repo** (fueron reportes de conversación). El último reporte versionado es `05.7-CANONICAL-MERGE-REPORT.md`.

## 2. Archivos promovidos (recovery → raíz)

| Origen (Clase) | Destino | Verificación |
|---|---|---|
| `recovery/reconstructed/worker.js` (B) | `worker.js` | sha256 promovido `79d79bf2…` + correcciones §6 |
| `recovery/reconstructed/wrangler.toml` (B/D) | `wrangler.toml` | única diff: `WHATSAPP_PHONE +5358385702` (valor público prod) |
| `recovery/production-d5a61b95/assets/*.html` ×9 (A) | `public/` | hash-por-hash verificado tras copia |
| `…/assets/favicon.ico` (A) | `public/favicon.ico` | nuevo |
| `…/assets/robots.txt` (A) | `public/robots.txt` | nuevo |
| `…/assets/icons/placeholder.svg` (A) | `public/icons/placeholder.svg` | nuevo |

12 módulos no-entry (`rate-limit.js`, `session-runtime.js`, `src/auth/authorization/*.js`): **idénticos** raíz ≡ reconstructed → sin copia. `public/config.js` e `icons/icon-*.png`: UNCHANGED (HEAD=prod) → conservados.

## 3. Archivos modificados (git)

`worker.js`, `wrangler.toml`, `public/{admin,comparar/index,ia/index,index,manifest,mapa/index,property,sw,variables.css}`, `recovery/reconstructed/worker.js`, `test/listing-identity.test.mjs`, `test/worker-integrity.test.mjs`.

## 4. Archivos añadidos

`public/favicon.ico`, `public/robots.txt`, `public/icons/placeholder.svg`, `reports/05.10-PRE-COMMIT-REPORT.md`.

## 5. Archivos eliminados

Ninguno. (Ninguna funcionalidad recuperada eliminada; baseline 24e0982 no es filtro de exclusión.)

## 6. Conflictos / discrepancias encontradas

**D1 — Defecto de reconstrucción (origen determinado y corregido):** `recovery/reconstructed/worker.js` llamaba `serializeProperty(row)` **sin** el argumento de audiencia en 6 call sites. La evidencia Clase-A (`recovery/production-d5a61b95/worker/original/worker.js`) muestra `serializeProperty(row, "public")` / `(row, "admin")`. Origen: pérdida del literal durante la restauración de helpers esbuild en 05.4. Impacto: whitelist de audiencia resolvía a `undefined` → serialización pública devolvía `null` (hallado por los tests, 14 fallos iniciales). **Corrección aplicada en `worker.js` y en `recovery/reconstructed/worker.js`** para no re-sembrar el defecto. Tras la corrección, paridad con bundle re-verificada (§13).

**D2 — Marcadores anti-drift CSP ausentes en el canónico:** `CSP_SCRIPT_SRC` re-embebido entre los marcadores `GENERATED CSP-SCRIPT-SRC:BEGIN/END` (existencia verificada: contenido hash idéntico al canónico; el guard exit 0). Aplicado a `worker.js` y `recovery/reconstructed/worker.js`. Sin este bloque, `security-headers.test.mjs` falla.

**D3 — Drift de tests (origen: escritos contra baseline legacy 24e0982):**
- `test/listing-identity.test.mjs`: mock `VECTORIZE` → `VECTOR_INDEX` (binding real per `script-settings.json`, Clase-A). Título del test queda como etiqueta descriptiva.
- `test/worker-integrity.test.mjs`: mock D1 sin `batch()` → el alta admin caía al path fallback (con prefijo `attempt` en binds). Se añadió `batch()` al mock para ejercitar el path primario `listing_id_sequence` (binds `values` puros, lat=14/lng=15). Comentario inline documenta el contrato.

Sin conflictos de merge (copias directas sobre archivos versionados; nada solapado).

## 7. Razón de cada cambio

- `worker.js` ← canonical: el entry legacy usaba `env.VECTORIZE` (roto en prod), carecía de sitemap, `/api/config` con `MARKET_CONFIG`, `extractAiText`, `normalizeCurrency`, filtros minPrice/neighborhood/q, rate-limit en admin verify, SEO enriquecido.
- `wrangler.toml` ← canonical: `WHATSAPP_PHONE` placeholder `+5350000000` → valor real de producción `+5358385702` (dato público, no secreto).
- Assets ← evidencia de producción (05.5-ASSETS-RECONCILIATION): 9 divergentes + 3 auxiliares; CSP hash-based los requiere exactos; sin placeholders externos (verificado 0 refs a unsplash/placehold/picsum; solo variables CSS locales).
- Tests: reconciliación documentada en D3; semanticamente neutras (mocks, no comportamiento de producción).

## 8. Entry point real del Worker

`wrangler.toml → main = "worker.js"` (raíz). Sin build propio; Wrangler bundlea desde la raíz. El workflow `.github/workflows/deploy.yml` ejecuta `npx wrangler deploy` tras `npm ci` + gates — **no requirió cambios**: la causa raíz de 05.9 era el contenido de `worker.js`, no el workflow. Entrypoint del próximo deploy = worker canónico promovido ✔.

## 9. Build / dry-run

`npx wrangler deploy --dry-run` → **PASS**:
- `Total Upload: 62.48 KiB / gzip: 15.12 KiB`
- `Read 19 files from the assets directory …/public`
- Smart Placement warning (config `[placement] mode = "smart"` activa, misma que producción)
- compatibility_date `2026-08-19`, esm bundle generado OK
- `env.VECTORIZE`: **0 ocurrencias** en código; `env.VECTOR_INDEX`: 9 call sites ✔

## 10. Tests

| Resultado | Valor |
|---|---|
| `node --check worker.js` | OK |
| Suite `npm test` (8 archivos) | **PASS 188 / FAIL 0 / SKIPPED 0** |
| Guard CSP (`generate-csp-hashes.mjs`) | OK — 9 hashes sincronizados |

Secuencia: 14 fallos iniciales (D1+D2+D3) → correcciones basadas en evidencia → 188/188.

## 11. Bindings (dry-run ≡ producción per script-settings.json)

| Binding | Recurso | Estado |
|---|---|---|
| `DB` | D1 `nexo-db` (`03a0f232-…`) | ✔ |
| `VECTOR_INDEX` | Vectorize `nexo-index` | ✔ |
| `BUCKET_IMAGENES` | R2 `nexo-media` | ✔ |
| `AI` | Workers AI | ✔ |
| `ASSETS` | `public/` (run_worker_first) | ✔ |
| `WHATSAPP_PHONE` | `+5358385702` (público) | ✔ |
| `SENTRY_DSN` | `""` plain (fail-open) | ✔ |
| `ADMIN_TOKEN` / `ADMIN_PASSWORD` | secret_text (solo servidor; no en wrangler.toml) | intactos, no rotados (fuera de alcance) |

Migrations: sin cambios (0001–0005 intactas; 0006 = producción-only, documentada en `recovery/notes/`, **no aplicable en esta fase**).

## 12. Seguridad

- Secrets: 0 hardcodeados en `worker.js`/assets (scan de literales + patrones de tokens ✔); `ADMIN_TOKEN`/`ADMIN_PASSWORD`/`SENTRY_DSN` solo vía bindings `env.*`.
- PII: whitelist serializer intacta; `security-headers.test.mjs` + `authorization.test.mjs` validan filtrado de `owner_name/owner_phone/internal_notes/address` en audiencia pública.
- XSS: `escHtml`/`escJson` presentes y usados en SEO injection; guard sin handlers inline (`on*=`) en HTML.
- SQL: parametrizado (35 statements, paridad 35/35 con producción, 0 concatenación de input).
- CSP: hash-based, 9 hashes, sin `unsafe-inline` en script-src; markers anti-drift restaurados.
- Bindings correctos (§11). auth/session/authorization intactos (módulos byte-idénticos a HEAD, 188 tests).
- Sin rotación de secrets, sin cambio de credenciales (fuera de alcance de esta fase).

## 13. Comparación contra d5a61b95 (solo lectura; sin deploy, sin writes)

Rebuild `esbuild worker.js --bundle` vs `recovery/production-d5a61b95/worker/original/worker.js`:
- **Rutas:** 11/11 EQUAL (`/api/health`, `/api/config`, `/api/properties`, `/api/properties/:id`, `/similar`, `/api/chat`, `/api/admin/properties` GET/POST, `/api/admin/properties/:id` PUT/DELETE, `/api/admin/verify`, `/api/session/status`, `/api/session/logout`, `/sitemap.xml`, `/media/*`, `/property.html` SEO).
- **SQL:** 35 statements prod vs 35 rebuilt — `only-in-prod: 0`, `only-in-rebuilt: 0`.
- **CSP hashes:** 9/9 idénticos a producción (`build-reproducibility.json`).
- Bundle size: rebuilt 60.5kb vs prod 64.0kb (delta = prelude/preludios esbuild y comentarios; ya clasificado BUILD-TRANSFORMATION en 05.5).

## 14. Funcionalidades recuperadas (checklist 05.9 en el nuevo build)

✔ SEO injection (`/property.html` canonical/OG/JSON-LD geo) · ✔ `/sitemap.xml` dinámico · ✔ `favicon.ico` · ✔ `MARKET_CONFIG` + `/api/config` · ✔ currency (`ALLOWED_CURRENCIES`, `normalizeCurrency`, `priceCurrency`) · ✔ `extractAiText` en `/api/chat` · ✔ Vectorize `VECTOR_INDEX` · ✔ `/api/admin/verify` con `enforceRateLimit` · ✔ rate limiting · ✔ filtros `q/minPrice/neighborhood` · ✔ `/media/` R2 con negociación WebP · ✔ serializer whitelist.

**Ninguna sigue ausente → se continúa.**

## 15. Funcionalidades todavía UNKNOWN

- Valores reales de secrets (`ADMIN_TOKEN`, `ADMIN_PASSWORD`) y `SENTRY_DSN` — solo nombres de binding (05.5 §E).
- Contenido exacto de `0006_properties_currency.sql` (solo efecto observable; documentado en `recovery/notes/`).
- Autor/fecha del working tree no versionado que originó d5a61b95 (prevenir con workflow: deploy solo desde commits).
- Etiquetas "04.7/04.8" de assets: no verificables como commits; clasificadas PRODUCTION-ONLY (05.5-ASSETS).

## 16. Riesgos

- **Bajo:** el defecto D1 demuestra que la comparación símbolo-nivel de 05.4 no capturó el literal de audiencia; mitigado por paridad rutas/SQL re-verificada (§13) y suite completa.
- **Bajo:** `SENTRY_DSN` plain vacío → fail-open intencional (doc). `ADMIN_PASSWORD` legacy pendiente de deprecación (roadmap, fuera de alcance).
- **Bajo:** warning de Smart Placement + `run_worker_first` es el mismo de producción actual (no regresión).
- **Nota operativa:** los tests ahora ejercitan el path primario `batch/listing_id_sequence`; el path fallback (sin `batch`) queda cubierto indirectamente por `authorization.test.mjs` (mock sin batch).

## 17. Recomendación para el siguiente paso

Working tree listo para **commit + push a `main`** (autorización requerida):

1. `git add -A` (worker.js, wrangler.toml, public/, recovery/reconstructed/worker.js, 2 tests, este reporte).
2. Commit propuesto: `fix(worker): promote canonical d5a61b95 source to root (NEXO 05.10)`.
3. Push → CI (`quality-audit` gates verificados localmente: assets 233.7KB < 800KB; index.html contiene skeleton/navigator.onLine/aria-label ✔) → `wrangler deploy` controlado.
4. Post-deploy: verificación `/api/health` + endpoints críticos + comparación de versión vs d5a61b95.

**STOP — sin commit, sin push, sin deploy, sin migrations, sin writes en D1/R2/Vectorize, producción intacta.**

_Contadores: push=0 · deploy=0 · D1 writes=0 · R2 writes=0 · Vectorize writes=0 · migrations=0 · secrets rotados=0._
