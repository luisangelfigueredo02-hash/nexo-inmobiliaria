# NEXO 05.5 — CANONICAL SOURCE RECONCILIATION & SALE-SAFE BASELINE REPORT

Periodo: 2026-08-22. Procedencia: branch `recovery/nexo-production-2026-08-22` (local, no push). `main` intacto en `24e0982`. Operación: **read-only** (Cloudflare API + HTTP GET público). Se detectaron y preservaron exclusivamente nombres de bindings, nombres de columnas, settings y metadatos de Cloudflare — **sin escrituras en producción**.

---

## 1. Canonical source identification

- Source of truth = `recovery/reconstructed/` (código) + `recovery/production-d5a61b95/` (artefactos). HEAD (`main`) = baseline legacy; canonical production-derived.
- Worker identity: `nexo-inmueble` / version `d5a61b95-…`, script_tag `ca01d3f579e042e98…`, account `8816663cf4f1768e51859f07ab8305f4`.
- Hashes de bundle: worker.js `93dc101c…`, multipart original `eef16f8e…`, entry reconstruct `79d79bf2…`, wrangler.toml `1a626a4c…`.
- Manifest completo: `reports/05.5-CANONICAL-SOURCE-MANIFEST.md`.

## 2. Worker reconciliation

- Funciones de HEAD ⊆ producción; producción añade `extractAiText`, `normalizeCurrency`.
- Todas las diferencias clasificadas: **PRODUCTION-EXACT / PRODUCTION-ONLY / HEAD-ONLY (tree-shaken) / BUILD-TRANSFORMATION / UNKNOWN**.
- Reporte: `reports/05.5-WORKER-RECONCILIATION.md`.

## 3. D1 schema reconciliation

- Tracker: migraciones 0001-0006 (0006 aplicada solo en producción; ADD COLUMN `currency TEXT` reconstruido/materializado en `recovery/notes/0006_properties_currency.sql` — NO ejecutable).
- Parity por tabla: 9 tablas en PARITY, `properties` en DIFF (currency), y tablas PRODUCTION-ONLY (analytics/ia/legacy + rate_limits runtime). Índices repo-only = dropped por rebuild de 0003 (previsto).
- Reporte: `reports/05.5-D1-SCHEMA-RECONCILIATION.md`.

## 4. Assets

- 9 assets production-only (+ 4 auxiliares: `favicon.ico`, `robots.txt`, `sitemap.xml` endpoint, `icons/placeholder.svg`). HEAD match: `config.js`, `icons/icon-192.png`, `icons/icon-512.png`.
- Canonical assets = `recovery/production-d5a61b95/assets/` más los matched heredados de HEAD. HEAD `sw.js` (`nexo-v3-stable`) vs producción `nexo-v5-perf` (diff verificado en `recovery/analysis/assets-parity.json`).
- Reporte: `reports/05.5-ASSETS-RECONCILIATION.md`.

## 5. Build reproducibility

- Rebuild con esbuild (recovery/reconstructed): routes EQUAL, SQL EQUAL, CSP EQUAL (9 hashes), bindings en dry-run EQUAL, tamaño 62,200 vs 64,059 (97.1%).
- Evidencia estructural: `recovery/analysis/build-reproducibility.json`.

## 6. Test coverage gap

- Historical **211 tests = NOT RECOVERABLE** (no existen en HEAD ni en producción).
- Current tests: **188/188 PASS** (node --test sobre HEAD). 14 gaps formales identificados; **no se escribieron nuevos tests** por obligación de la fase.
- Reporte: `reports/05.5-TEST-COVERAGE-GAP.md`.

## 7. Security audit

- No secrets hardcodeados; no PII privada expuesta en endpoints públicos; no endpoints debug extra; no eval/dynamic-SQL; teléfono `+5358385702` clasificado como público de mercado (configuración).
- Sin hallazgos ROTATION REQUIRED en el código recuperado. Las tokens de host expuestas (`~/.cf_token`, `~/.gh_token`) quedan en el backlog de rotación (y son fuera de ownership del repo).
- Reporte: `reports/05.5-SECURITY-AUDIT.md`.

## 8. Branch confirmation

- Branch canónico: `recovery/nexo-production-2026-08-22` (local), cadena `0fd6eb5 → 0425c1e → 24e0982 (main)`; **main protegido en `24e0982`**.

## 9. TAKEOVER

- `recovery/reconstructed/TAKEOVER.md` (todo categorizado VERIFIED/RECOVERED/RECONSTRUCTED/UNKNOWN).

## 10. Drift prevention policy

- `reports/05.5-DRIFT-PREVENTION-POLICY.md` (docs only; no se añadieron hooks al repo).

## 11. Commit: `bd00c22` (docs(recovery): NEXO 05.5 canonical source reconciliation and sale-safe baseline)
## 12. Push: **NO**
## 13. Deploy: **NO**
## 14. D1 writes: **NO**
## 15. R2 writes: **NO**
## 16. Vectorize writes: **NO**

## VEREDICTO

**RECOVERY COMPLETED.**

- Recuperación ejecutada únicamente en la rama recovery; nada fue aplicado a producción.
- La brecha producción ↔ git quedó diagnosticada completamente; la reconstrucción es determinista (read-only) y no se aplicó ningún cambio.
- Sale-safe baseline: los cambios quedan exclusivamente en la rama `recovery/nexo-production-2026-08-22`; main permanece en `24e0982`.

STOP AFTER THIS REPORT.